describe('mutation', () => {
  it('should work', () => {});

  it('should work with signals', () => {});

  it('should be typed', () => {});

  it('should work with callback handlers', () => {});
});
